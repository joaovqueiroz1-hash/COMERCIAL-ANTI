# Copie para .env e preencha. NUNCA versione o .env real.
# Estas VITE_* vão para o bundle do navegador — use SÓ chaves públicas aqui.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""

# NÃO coloque chaves privadas com prefixo VITE_ (elas vazam no site).
# service_role, senha de banco, chave Anthropic/OpenAI, token Z-API:
# só no servidor (Supabase Edge Functions / Vercel env sem prefixo VITE_).
